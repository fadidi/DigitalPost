- content_for :backtrack do
  %li= link_to 'Work Zones', work_zones_path
- content_for :top_right_menu do
  %li.active= link_to raw('<span class="icon-plus"></span> New Work Zone'), new_work_zone_path if can? :create, WorkZone

= render 'form'
