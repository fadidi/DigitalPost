%ul.nav.nav-pills.pull-right
  %li= link_to 'Everyone', users_path
  %li= link_to 'Volunteers', volunteers_path
  - if @volunteer && can?(:edit, @volunteer.user)
    %li
      = link_to edit_user_path(@volunteer.user) do
        %span.icon-pencil
        Edit #{@volunteer.user.name}
