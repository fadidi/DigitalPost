- content_for :body do
  .container-fluid
    .row-fluid
      .span12
        %ul.nav.nav-pills.pull-left
          %li= link_to 'Home', root_path
          = render 'breadcrumps' rescue nil
        %ul.nav.nav-pills.pull-right
          = render 'menu' rescue nil
    .row-fluid
      .span2
        = render 'shared/menu'
      .span10
        .row-fluid
          .span12
            = yield
= render :template => 'layouts/application'
