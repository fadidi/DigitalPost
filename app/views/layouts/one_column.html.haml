- content_for :body do
  .container-fluid
    .row-fluid
      .span12
        = render 'layouts/backtrack'
    .row-fluid
      .span12
        = yield
  - if Rails.env.development?
    .container-fluid
      .row-fluid
        .span12= debug(params)
= render :template => 'layouts/application'
