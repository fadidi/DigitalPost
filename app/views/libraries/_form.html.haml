= form_for @library do |f|
  = render 'shared/form_errors', :item => @library if @library.errors.any?
  = render 'shared/form_info'
  .row-fluid
    .span5
      = f.label :name
      = f.text_field :name, :class => 'required span12'
      = f.label :description
      = f.text_area :description, :class => 'span12', :rows => 4
      = f.label :restricted, :class => 'checkbox' do
        = f.check_box :restricted
        = t 'activerecord.attributes.library.restricted'
      = f.label :shared, :class => 'checkbox' do
        = f.check_box :shared
        = t 'activerecord.attributes.library.shared'
      = f.label :official, :class => 'checkbox' do
        = f.check_box :official
        = t 'activerecord.attributes.library.official'
    .span2
      = image_tag(@library.photo.golden374.url)
    .span5
      = f.label :photo
      = f.file_field :photo, :class => 'span12'
      - if @library.photo?
        = f.label :remove_photo, :class => 'checkbox' do
          = f.check_box :remove_photo
          = t 'activerecord.attributes.library.remove_photo'
      %span.help-block the photo will be used to visually identify this #{Library.model_name.human.downcase}
  .form-actions
    = f.submit 'Save'
